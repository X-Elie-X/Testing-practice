const reverse = require('./Task2');

test('reverse string', () => {
  expect(reverse('terminal')).toBe('lanimret');
});

test('reverse string', () => {
  expect(reverse('output')).toBe('tuptuo');
});

test('reverse string', () => {
  expect(reverse('terminal')).toBe('lanimret');
});

test('reverse string', () => {
  expect(reverse('task')).toBe('ksat');
});

test('reverse string', () => {
  expect(reverse('string')).toBe('gnirts');
});
